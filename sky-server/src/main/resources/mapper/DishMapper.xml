<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (category_id, name, image, price, description, status, create_time, update_time, updateUser, createUser)
        values (#{categoryId}, #{name}, #{image}, #{price}, #{description}, #{status}, #{createTime}, #{updateTime},#{updateUser},#{createUser})
    </insert>
</mapper>